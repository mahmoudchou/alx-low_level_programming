#include <stdio.h>

/**
 * main - A programm that print a line with a put function
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
